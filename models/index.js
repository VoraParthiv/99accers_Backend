const addProperty = require("./addProperty")
const subProperty = require("./subProperty")
const allPropertyInfo = require("./allPropertyInfo")

module.exports = {
    addProperty,
    subProperty,
    allPropertyInfo,
}
